import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['fastly.picsum.photos', 'kpwssornmybpvasbnpvl.supabase.co','img.clerk.com'],
  },
};

export default nextConfig;
